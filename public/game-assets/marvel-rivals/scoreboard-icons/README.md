Add manually sourced Marvel Rivals scoreboard icons here using:

`{hero-id}/{costume-id}.png`

Example:

`thor/default.png`

The extraction matcher skips missing files, so this folder can be partially populated without breaking the app.
