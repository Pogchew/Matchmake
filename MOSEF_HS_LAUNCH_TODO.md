# MOSEF / High School Fall Launch TODO

This checklist is for preparing Matchmake for MOSEF / high-school usage this fall.

Working rules for Codex sessions:
- Work on one unchecked task at a time unless the task is explicitly a small checklist group.
- Do not batch unrelated tasks together.
- Preserve existing app behavior unless the task requires a behavior change.
- Run focused QA for the touched area before checking off a task.
- Update this file after each completed or deferred task.
- If a task is blocked, add a short note under that task instead of skipping ahead silently.

## P0 - Extractor API Security and Stability

- [x] Require authenticated access for `POST /api/postgame/extract`.
- [x] Add per-user or per-org rate limiting for screenshot extraction.
  - Added per-user screenshot extraction limits in `POST /api/postgame/extract`: default `20` requests per authenticated user per hour, tunable with `POSTGAME_EXTRACT_RATE_LIMIT_MAX_REQUESTS` and `POSTGAME_EXTRACT_RATE_LIMIT_WINDOW_MS`; exceeded requests return `429` with retry/rate-limit headers.
- [x] Add upload size limits before buffering uploaded images.
  - Added an extractor upload cap in `POST /api/postgame/extract`: default `8 MB`, tunable with `POSTGAME_EXTRACT_MAX_UPLOAD_BYTES`; oversized `Content-Length` is rejected before multipart parsing when available, and oversized uploaded `image.size` is rejected before `arrayBuffer()` with `413`.
- [x] Add strict image type validation and clear `400` responses for malformed uploads.
  - Added extractor upload validation for supported image MIME types (`avif`, `bmp`, `gif`, `jpeg`, `png`, `webp`), byte-signature matching, and `sharp.metadata()` decode checks before optimization/Gemini calls; unsupported or unreadable uploads now return `400` with `unsupported_image_type` or `malformed_image_upload`.
- [x] Replace malformed-request `500` responses with appropriate `400`, `401`, or `403` responses.
  - Wrapped extractor multipart parsing and uploaded file buffering so malformed form bodies return `400 malformed_form_data`, unreadable image bodies return `400 malformed_image_upload`, and auth failures continue to return `401 authentication_required` instead of falling into the outer unexpected `500`.
- [x] Remove or gate verbose production extractor logs, especially raw Deadlock response logging.
  - Added an extractor debug log gate (`NODE_ENV=development` or `POSTGAME_EXTRACT_DEBUG_LOGS=true`) and moved raw Gemini previews, Deadlock raw-response logging, Deadlock extraction summaries, Marvel normalization debug data, and model failure bodies behind it; production logs now keep only sanitized operational status/error details by default.
- [x] Add focused API tests or verification commands for unauthenticated, malformed, oversized, and valid image requests.
  - Added `npm run verify:postgame-extract-api` / `scripts/verify-postgame-extract-api.mjs` to verify `401 authentication_required`, authenticated `400 malformed_form_data`, authenticated `413 upload_too_large`, and optional authenticated valid-image extraction via `MATCHMAKE_EXTRACT_RUN_VALID=true` with a supplied scoreboard image.

## P0 - Supabase RLS and Data Access Audit

- [x] Audit production Supabase policies against `rls_policies.sql`, `public_read_policies.sql`, and newer scoped policy files.
  - Documented the production audit in `docs/supabase-rls-production-audit-2026-06-16.md`: live Supabase advisors still report broad MVP `true` insert/update policies on `users`, `organizations`, `teams`, and `scrim_requests`; RLS is enabled on visible live public tables; migrations list is empty; analytics mirror tables and the `org-logos` bucket do not appear active in the live snapshot.
- [x] Replace broad `using (true)` / `with check (true)` MVP policies with org-scoped policies.
  - Replaced the broad authenticated baseline policies in `rls_policies.sql` with idempotent scoped policies for `users`, `organizations`, `teams`, and `scrim_requests`; applied them to production Supabase along with the existing narrower scrim request workflow policies. Supabase security advisors now report no always-true RLS findings; only leaked-password protection remains.
- [x] Confirm anon public-read policies are removed or intentionally limited for production.
  - Replaced the temporary MVP anon/public `using (true)` read policies with limited unauthenticated Scrim Board policies in `public_read_policies.sql`; applied them to production Supabase. Anon now has column-level `SELECT` only for public board fields on `organizations`, `teams`, and `scrim_requests`, with RLS limited to open/unmatched scrim listings and their posting teams/orgs. Supabase advisors still report no RLS always-true findings.
- [x] Confirm users can only update their own org, teams, roster data, requests, and reviews.
  - Verified production RLS as an authenticated user in rollback transactions: own profile/org/team roster/open listing/match review updates succeeded, while cross-org profile/org/team roster/match review updates returned zero rows. Fixed a recursive team policy path by moving core RLS ownership checks into private security-definer helpers, and added a `scrim_requests` update trigger to reject cross-org metadata-only request updates while preserving the intended open-to-pending request transition. Supabase advisors still report no RLS always-true findings.
- [x] Confirm match reviews, chats, rosters, and team metadata are not cross-org readable.
  - Verified production RLS as an authenticated user in rollback transactions: own review/team/roster rows and participant chat were readable, while cross-org reviews, full team/roster rows, and a temporary non-participant chat message were not readable. Tightened authenticated `organizations` and `teams` read policies to own-org only; unauthenticated public Scrim Board access remains limited by `public_read_policies.sql` column-level grants and open-listing RLS. Supabase security advisors still report no RLS always-true findings; only leaked-password protection remains.
- [x] Document the final production RLS policy set and how to apply it safely.
  - Added `docs/supabase-production-rls-policy-runbook-2026-06-17.md` with the current production policy inventory, ordered repo SQL apply sequence, rollback guidance, verification SQL, focused access QA checklist, and Supabase source references. Verified live production RLS/policy/grant/function/trigger state and security advisors while drafting the runbook.

## P0 - School Privacy and Launch Docs

- [x] Draft a school-facing Privacy Policy.
  - Added `docs/school-facing-privacy-policy-draft.md` as a school/district review draft covering Matchmake data categories, school-authorized use, public Scrim Board visibility, service providers, COPPA/FERPA framing, student/parent controls, retention, security, and publication checklist items that still need owner/counsel decisions.
- [x] Draft Terms of Service.
  - Added `docs/terms-of-service-draft.md` as a school/district review draft covering parties/contact placeholders, school-authorized use, eligibility and under-13 dependencies, account/security rules, org/team administration, scrim/chat expectations, match review and screenshot extraction terms, user content, privacy/student data, service providers, acceptable-use baseline, enforcement, availability, disclaimers, and counsel placeholders for fees, liability, indemnity, governing law, and disputes.
- [x] Draft Acceptable Use Policy for student/team behavior.
  - Added `docs/acceptable-use-policy-draft.md` as a student/coach-facing behavior draft covering account access, roster/profile rules, scrim posting and requests, active scrim chat, match review and screenshot conduct, public Scrim Board/calendar link safety, reporting/escalation placeholders, enforcement options, and coach/student responsibilities.
- [x] Draft a School/District Data Privacy Agreement template.
  - Added `docs/school-district-data-privacy-agreement-template.md` as a district/counsel review template covering parties, authorized use, FERPA school-official framework, COPPA/under-13 dependencies, data elements, ownership/control, prohibited uses, subprocessors, security, breach notice, access/correction/export/deletion support, retention/return/destruction, de-identified data, public fields/directory information, student communications, service changes, audit/documentation, legal compliance, signatures, and appendices.
- [x] Define data deletion and export process for schools/orgs.
  - Added `docs/school-data-deletion-export-process.md` as a launch process covering request intake/verification, response targets, current Supabase data inventory, export package contents, export SQL checklists, account/team/org/targeted deletion modes, launch retention windows, production deletion QA, completion confirmation language, owner decisions, and official ED/FTC references.
- [x] Define under-13 / COPPA handling and whether under-13 users are allowed.
  - Added `docs/under-13-coppa-handling.md` defining the MOSEF / high-school pilot decision: no self-service under-13 use; under-13 access only after written school/district authorization, COPPA notice/consent path confirmation, school-authorized use limits, and parent/guardian review/deletion handling. Updated the Privacy Policy, Terms, DPA, AUP, and deletion/export process drafts to reference that decision.
- [x] Add clear no-sale/no-advertising use language for student data.
  - Added `docs/student-data-no-sale-no-ads.md` and strengthened the Privacy Policy, Terms, DPA, and AUP drafts with a clear student-data commitment: no sale, no rent/trade/license to advertisers or data brokers, no targeted/behavioral advertising or retargeting, no advertising/marketing profiles, no marketing to students/parents/households, and service-provider use limited to providing/supporting Matchmake unless a school expressly authorizes otherwise in writing.

## P0 - Extractor UI Regression QA

- [x] Test League of Legends screenshot extraction through the actual browser UI.
  - Retried 2026-06-18 after the earlier Gemini `503` block. Actual browser UI flow passed on local dev using `/team/be779b0a-504d-4b54-8725-9d9f6110e20f/dashboard?new=true` and `/tmp/matchmake-league-scoreboard-qa.jpg`: upload control rendered, `POST /api/postgame/extract` returned `200`, the dashboard populated League team/opponent rows, and the editable review form contained expected player/stat values such as `Painwright MTFK`, `Kled`, and `7/2/15`. Some extracted fields were correctly flagged `Needs review`; save/deep-stats validation remains for later TODO items. Evidence screenshots: `/tmp/matchmake-league-retry-before.png`, `/tmp/matchmake-league-retry-after.png`, `/tmp/matchmake-league-retry-editor.png`.
- [x] Test Valorant screenshot extraction through the actual browser UI.
  - FAILED QA 2026-06-18: Actual browser UI flow reached `/team/fc8f1758-037a-4a3e-902e-b7591010db71/dashboard?new=true`, uploaded `/tmp/matchmake-valorant-scoreboard-qa.png`, and `POST /api/postgame/extract` returned `200`, but extracted data did not fully match the visible screenshot. The source screenshot visibly shows `Goodseed` with `45/35/18`, while the populated dashboard/editor showed `Goodseed` as `45/35/10`. Leave this unchecked until the Valorant extractor correctly preserves visible K/D/A values. The previous hardcoded local fixture `/Users/nicholaschiu/Downloads/220695198-47f6b995-b1e4-4fc8-83f6-46325065e388.png` was missing, so this retry used a public Dot Esports/Imgur Valorant scoreboard screenshot. Evidence screenshots: `/tmp/matchmake-valorant-before.png`, `/tmp/matchmake-valorant-after.png`, `/tmp/matchmake-valorant-editor.png`.
  - FIXED QA 2026-06-18: Hardened Valorant extraction to read K/D/A only from the slash-separated KDA column between ACS and econ rating, and to validate extracted agent names against the local Valorant reference list from `public/valorant/agents`. Reran authenticated extractor API QA with `/tmp/matchmake-valorant-scoreboard-qa.png`: `POST /api/postgame/extract` returned `200`, sent 2 Valorant reference parts, and returned `Goodseed` as `Raze`, ACS `271`, KDA `45/35/18`, econ `50`, first bloods `4`, plants `5`, defuses `1`. Reran actual browser UI upload on `/team/fc8f1758-037a-4a3e-902e-b7591010db71/dashboard?new=true`; dashboard and editable review form contained `Goodseed`, `Raze`, `271`, `45`, `35`, `18`, `50`, `4`, `5`, and `1`. Evidence screenshots: `/tmp/matchmake-valorant-fix-before.png`, `/tmp/matchmake-valorant-fix-after.png`, `/tmp/matchmake-valorant-fix-editor.png`.
- [x] Test Marvel Rivals screenshot extraction through the actual browser UI.
  - Completed 2026-06-18 after recovering the missing local fixture from the public Reddit/i.redd.it source into `/tmp/matchmake-marvel-rivals-scoreboard-qa.jpeg`. Hardened the Marvel prompt to preserve the saved stat column order after Medals (`final_hits`, `damage`, `damage_blocked`, `healing`, `accuracy_percent`) and adjusted client pre-upload resizing to skip recompression for already-small images so scoreboard digits are not degraded before the server optimizer. Actual browser UI flow passed on local dev using `/team/23fb473a-f046-4b9e-96c1-5da93d1d46d4/dashboard?new=true`: upload control rendered, `POST /api/postgame/extract` returned `200`, the server normalized 12 rows with 6/6 team grouping, and the editable review form contained visible source values including `Lucnif`, KDA `6/9/0`, final hits `3`, damage `4554`, blocked `3237`, healing `0`, accuracy `33`, map `KLYNTAR: SYMBIOTIC SURFACE`, objective `CONVERGENCE`, and duration `00:07:05`. Hero names remain AI-suggested/review-needed in this fixture, so the later character-field verification item should still scrutinize Marvel hero accuracy before save/deep-stats QA. Evidence screenshots: `/tmp/matchmake-marvel-rivals-jpeg-before.png`, `/tmp/matchmake-marvel-rivals-jpeg-after.png`, `/tmp/matchmake-marvel-rivals-jpeg-editor.png`.
- [x] Test Overwatch 2 screenshot extraction through the actual browser UI.
  - Completed 2026-06-18 after recovering the missing local fixture from the public AltChar scoreboard image into `/tmp/matchmake-overwatch2-scoreboard-qa.jpg`. Actual browser UI flow passed on local dev using `/team/6ce198b2-3180-46a9-b0db-2befd27d7aad/dashboard?new=true`: upload control rendered, `POST /api/postgame/extract` returned `200`, and the editable review form populated visible source values including `Circuit Royal · Escort`, duration `2:48`, Junker Queen row `6/3/2` with damage `1928`, healing `173`, mitigation `260`, the Junkrat/WEJENSEN row `7/3/2` with damage `2241`, and opponent rows such as Reinhardt `6/0/2` with mitigation `1882`. The right-side Junkrat detail panel was not copied into saved row stats; final-blow/objective-kill fields remained `Needs review`, which is acceptable for this UI extraction pass and remains covered by later save/character/stat verification tasks. Evidence screenshots: `/tmp/matchmake-overwatch2-before.png`, `/tmp/matchmake-overwatch2-after.png`, `/tmp/matchmake-overwatch2-editor.png`.
- [x] Test Deadlock screenshot extraction through the actual browser UI.
  - Completed 2026-06-18 after recovering the missing local fixture from the public esports.gg Deadlock replay screenshot into `/tmp/matchmake-deadlock-scoreboard-qa.jpg`. Initial actual browser UI QA found a real duration OCR miss: the source image shows top-center duration `51:50`, while extraction populated `31:50`. Added a Deadlock-specific zoomed top-center header crop to the extractor request so Gemini reads `match.duration` from the enlarged duration area while preserving full-scoreboard row extraction. Reran actual browser UI upload on local dev using `/team/7f034135-de5b-477a-a218-14c3d187db83/dashboard?new=true`; `POST /api/postgame/extract` returned `200`, the dashboard populated 12 Deadlock rows, and the editable review form contained expected source values including `Ceejay`, `McGinnis`, duration `51:50`, souls `55777`, K/D/A `14/17/18`, player damage `62114`, objective damage `15164`, and healing `14617`. Evidence screenshots: `/tmp/matchmake-deadlock-before.png`, `/tmp/matchmake-deadlock-after.png`, `/tmp/matchmake-deadlock-editor.png`.
- [x] Verify extracted player rows, team/opponent grouping, stats, scores, maps, dates, and character fields save correctly.
  - Completed 2026-06-18 with actual browser upload + save QA against Supabase for the five priority fixtures. The save pass created identifiable standalone QA review rows because this environment has no service-role/direct DB credentials for cleanup; rows are marked with `Codex save QA ...` notes. Verified saved `team_comp`/`opponent_comp` row counts and grouping, `player_rows`/`opponent_rows` mirrors, stats, scores, maps/modes, `played_at`, and representative character fields for League (`c2c16aa0-3b4d-4f1d-98df-46d15b20f1cb`), Valorant (`de8cd18c-b018-4f47-9807-499cbeba3ef2`), Marvel Rivals (`17061e72-5b39-4bed-ba34-7e38e81e38b1`), Overwatch 2 (`83f0abd9-52df-4ddf-b200-6d065e603403`), and Deadlock (`85d3f8a8-83d6-486a-b14d-1f4510795d13`). Fixed two issues found during QA: extracted invalid date placeholders such as `Needs review` now fall back to a valid current datetime before save, and Valorant extraction now applies a local high-confidence row-portrait icon match against `public/valorant/agents` so hallucinated agent names can be corrected before the review form/save payload. Valorant Goodseed saved as Raze with `45/35/18`, ACS `271`, and map `Split`; Deadlock saved no traditional score while preserving duration `51:50`; Marvel saved app-schema accuracy as `accuracy: 33`.
- [x] Verify saved extracted data appears correctly in Team Stats and Deep Stats.
  - Verified 2026-06-19 against the saved `Codex UI League` extraction in production Supabase: both persisted reviews loaded in Team Stats, and Deep Stats rendered the saved team/opponent aggregates, 5 player rows per side, champion breakdowns, K/D/A, gold, game length, and differentials.
  - Fixed signed compact formatting so a negative gold gap renders as `-11.3k` instead of `--11.3k`, and fixed the League coach read to describe an opponent kill lead as a positive magnitude (`17.0 kills`).
  - Focused QA passed: authenticated browser interaction from Overview to Deep Stats with no relevant console warnings/errors, `npx eslint src/app/team/page.js`, `npm run build`, and `git diff --check`. The in-app browser screenshot capture timed out, so DOM snapshots supplied the rendered evidence.
- [x] Add or update regression fixtures/scripts for the five priority games where practical.
  - Added `fixtures/postgame-extraction/five-priority-games.json` with deterministic League of Legends, Valorant, Marvel Rivals, Overwatch 2, and Deadlock completion fixtures derived from the browser-QA cases, plus `scripts/verify-postgame-regression-fixtures.mjs` and `npm run verify:postgame-regression`. The local verifier checks exact representative match/row values, expected team grouping and row counts, required completed fields, and review fallback behavior without depending on Gemini, authentication, or personal screenshot paths; existing live screenshot API/UI scripts remain available for optional OCR coverage.

## P0 - Minor Safety and Moderation

- [x] Confirm scrim chat visibility is limited to active scrim participants.
  - Verified 2026-06-21 against production Supabase RLS. `scrim_messages` has RLS enabled with authenticated-only read/insert policies limited to users whose organization owns the posting or matched team and whose request status is `pending` or `confirmed`. Rollback role simulations against a confirmed scrim showed all 3 messages to both participating organizations, 0 to an authenticated outsider, and 0 to `anon`; a posting-organization user also saw 0 messages after a separate scrim reached `completed`.
- [x] Add or document a report/escalation process for inappropriate chat or team behavior.
  - Added `docs/inappropriate-behavior-reporting-escalation.md` with a school-first and Matchmake support intake path, emergency handling, severity levels and response targets, evidence/privacy requirements, school/platform ownership, anti-retaliation guidance, a case workflow, closure outcomes, current product limitations, and a pre-launch checklist. Updated the Acceptable Use Policy to reference the runbook and explicitly document that the pilot has no in-app report path yet.
- [x] Add admin guidance for removing teams, users, scrims, or messages when needed.
  - Added `docs/admin-removal-guidance.md` with a current-control matrix, pre-action safeguards, exact user/team/scrim/message procedures, production-verified cascade and `SET NULL` effects, Auth/session caveats, read-only verification queries, post-action records, and explicit dashboard limitations. Linked it from the behavior escalation and school data deletion/export processes.
- [x] Confirm student emails are not exposed in public or cross-org surfaces.
  - Verified 2026-06-24 against production Supabase and the client query surface. `public.users` RLS exposes a profile only to that user (with a separate owner-only admin policy); a rollback simulation returned 1 own profile row and 0 cross-org profile/email rows. Revoked all `anon` privileges on `public.users`, confirmed anonymous table/column email reads are unavailable, and found no email-bearing public views/functions or existing email-like scrim chat sender names. Removed the scrim chat fallback that could have persisted `authUser.email` when a display name was missing; chat now uses the neutral `Teammate` fallback.
- [x] Review optional profile links for safety expectations and add guidance if needed.
  - Completed 2026-06-24. Verified production profile-link visibility and current data without exposing link values: `teams.roster_profiles` and `users.external_profile_urls` are not selectable by `anon`, a rollback cross-org team simulation returned 1 own-org team and 0 cross-org roster-profile values, production had 0 roster profile links, and existing user external profile links were HTTPS. Added `docs/profile-link-safety-guidance.md`, linked it from the Acceptable Use Policy, and tightened the roster editor so optional profile links must be complete HTTPS URLs, must be 500 characters or fewer, cannot include embedded sign-in information, and are not rendered as clickable links unless they pass validation. Focused QA passed with `npx eslint src/app/team/page.js`, `npm run build`, `git diff --check`, Supabase security advisors, and local browser route-health checks; authenticated roster-form interaction was not browser-tested because no test credentials are configured in this environment.

## P1 - Dependency and Production Security Cleanup

- [x] Review `npm audit --audit-level=moderate` findings.
  - Completed 2026-06-24. `npm audit --audit-level=moderate` reports 3 moderate findings across two advisory groups: `js-yaml <=4.1.1` and Next's bundled `postcss <8.5.10`. `js-yaml@4.1.1` is dev-only through `@eslint/eslintrc@3.3.5` -> `eslint@9.39.4`; it is absent from `npm audit --omit=dev`, and `npm audit fix` reports a non-force fix path. The production-impacting finding is `next@15.5.19` bundling `postcss@8.4.31` under `node_modules/next/node_modules/postcss`; the root `postcss@8.5.10` is already patched, but `npm audit --omit=dev --audit-level=moderate` still reports the Next-bundled copy as 2 moderate entries (`postcss` and `next`). Do not run `npm audit fix --force` blindly: npm proposes installing `next@9.3.3`, which is a breaking downgrade. QA commands run: `npm audit --audit-level=moderate`, `npm audit --omit=dev --audit-level=moderate`, `npm ls js-yaml postcss next --all`, `npm explain js-yaml`, `npm explain postcss`, and npm registry version checks for `js-yaml`, `postcss`, `next`, `next@canary`, `eslint`, and `@eslint/eslintrc`. Next steps remain the separate checklist items for safely addressing `js-yaml`, then Next/PostCSS.
- [x] Safely address `js-yaml` advisory without breaking the app.
  - Completed 2026-06-24. Ran `npm audit fix` without `--force`, which updated only the transitive dev dependency lockfile entry from `js-yaml@4.1.1` to `js-yaml@4.2.0` under `@eslint/eslintrc` / `eslint`; no runtime dependency or Next downgrade was applied. Verified `npm ls js-yaml @eslint/eslintrc eslint --all` resolves `js-yaml@4.2.0`, `npm audit --audit-level=moderate` no longer reports the `js-yaml` advisory, and the remaining audit findings are only the separate Next/PostCSS advisory. Focused QA passed: `npx eslint src` (existing custom-font warnings only), `npm run build` (existing workspace-root and custom-font warnings only), `npm audit --omit=dev --audit-level=moderate` (still reports only Next/PostCSS), and `git diff --check -- package-lock.json MOSEF_HS_LAUNCH_TODO.md`.
- [x] Safely address Next/PostCSS advisory by upgrading Next or documenting why it is blocked.
  - Completed 2026-06-25 as a documented blocker rather than a package upgrade. Current production audit still reports Next's bundled `postcss@8.4.31` under installed `next@15.5.19`; the root app `postcss@8.5.10` is already patched. Checked current npm release metadata: stable `next@16.2.9` and `next@backport` / `16.2.8` still pin `postcss@8.4.31`, while `next@16.3.0-canary.67` pins patched `postcss@8.5.10`. Did not move the launch app to canary because canary releases are not recommended for production and Next 16 also carries migration risk for this app's `src/middleware.js` and request APIs. Did not run `npm audit fix --force` because npm still proposes a breaking downgrade to `next@9.3.3`. Next safe action is to wait for a stable/backport Next release that bundles `postcss >=8.5.10`, then upgrade and run the dependency QA gate.
- [x] Re-run `npm audit`, lint, and build after dependency updates.
  - Completed 2026-06-25 after the `js-yaml` dependency update and the documented Next/PostCSS blocker. `npm audit --audit-level=moderate` and `npm audit --omit=dev --audit-level=moderate` both now report only the known Next-bundled `postcss@8.4.31` finding; the `js-yaml` advisory is gone. `npm ls js-yaml next postcss --all` confirmed `js-yaml@4.2.0`, root `postcss@8.5.10`, and `next@15.5.19 -> postcss@8.4.31`. Source lint passed with `npx eslint src` with only the existing `src/app/layout.js` custom-font warnings. `npm run build` passed with the existing workspace-root warning and the same custom-font warnings. Also ran `npm run lint`; it still fails because the repo-wide eslint script scans generated `.claude/worktrees/**/.next` bundles and reports `@next/next/no-assign-module-variable` in generated webpack files, so `npx eslint src` remains the focused source lint gate until those generated paths are excluded or removed.
- [x] Resolve or explicitly document existing layout font warnings.
  - Completed 2026-06-25. Removed the Google Fonts stylesheet links from `src/app/layout.js`, moved Inter to `next/font/google`, and replaced the Material Symbols external stylesheet with a local `@font-face` in `src/app/globals.css` backed by `public/fonts/material-symbols-outlined.ttf`. The existing `MaterialSymbol` component API remains unchanged. Focused QA passed: no remaining `fonts.googleapis`, `fonts.gstatic`, `Material+Symbols`, or Tailwind `font-['Inter']` references in source; `npx eslint src/app/layout.js src/components/MaterialSymbol.js`; `npx eslint src`; and `npm run build`. The build no longer emits the previous `@next/next/no-page-custom-font` warnings; the existing multiple-lockfile workspace-root warning remains.

## P1 - Production Environment Readiness

- [x] Confirm production Supabase project is separate from development data.
  - Blocked 2026-06-25: current evidence says production is not separated from development/QA data yet. Local `.env.local`, `.env.local.example`, and `DEPLOYMENT.md` all point to Supabase project `urrnrcdxekhovsemeuly`, documented as production. Supabase MCP project listing shows only one Matchmake project in the account (`urrnrcdxekhovsemeuly`, name `Matchmake`, status `ACTIVE_HEALTHY`); no separate Matchmake dev/staging Supabase project or local `supabase/config.toml` link was found. Read-only production checks showed app data in that same project and 7 `Codex%` QA review rows, matching earlier extractor QA notes. Next action: create or designate a separate development/staging Supabase project, wire local/dev env files to it, keep production deployment env pointed at `urrnrcdxekhovsemeuly`, and rerun this confirmation.
  - Still blocked 2026-06-25: local recheck found `.env.local` and `.env.local.example` still pointing at production project `urrnrcdxekhovsemeuly`, and no `supabase/config.toml`, `.vercel/project.json`, `.mcp.json`, or Supabase temp link was present. Live Supabase MCP project recheck could not run because the connector token is expired (`token_expired`). Re-authenticate Supabase MCP and create/designate a separate dev/staging project before rerunning this item.
  - Still blocked 2026-06-26: local recheck again found only `.env.local` and `.env.local.example`, both pointing at production project `urrnrcdxekhovsemeuly`; `DEPLOYMENT.md` still documents that same project as production and says local development should move to a non-production Supabase project once one exists. Supabase MCP project listing is available again and shows one Matchmake project (`urrnrcdxekhovsemeuly`, `ACTIVE_HEALTHY`) plus an unrelated `FitCheck` project; no separate Matchmake development/staging project was visible. Supabase branch listing for the production project failed inside the connector (`Project reference is missing when validating permissions`), so branch separation could not be confirmed. Next action remains: create or designate a separate Matchmake dev/staging Supabase project or branch, point local/dev env files at it, and rerun this confirmation before checking off the item.
  - Still blocked 2026-06-27: local recheck again found only `.env.local` and `.env.local.example`, both pointing at production project `urrnrcdxekhovsemeuly`, and no `supabase/config.toml`, `.vercel/project.json`, or `.mcp.json` project link was present. `DEPLOYMENT.md` still documents `urrnrcdxekhovsemeuly` as production and says local development should move to a non-production Supabase project once one exists. Live Supabase MCP project listing still shows one Matchmake project (`urrnrcdxekhovsemeuly`, `ACTIVE_HEALTHY`) plus unrelated `FitCheck`; branch listing for the Matchmake project still fails with `Project reference is missing when validating permissions`. Read-only production SQL still shows 7 `Codex%` QA review rows in `public.team_match_reviews`. Next action remains: create/designate a separate Matchmake dev/staging project or branch, point local/dev env files at it, and rerun this confirmation before checking off the item.
  - Still blocked 2026-06-28: local recheck still found only `.env.local` and `.env.local.example`, both pointing at production project `urrnrcdxekhovsemeuly`; no `supabase/config.toml`, `.vercel/project.json`, `.mcp.json`, or other local project/branch link was found. `DEPLOYMENT.md` still documents `urrnrcdxekhovsemeuly` as the current production Supabase project and says day-to-day development should prefer a non-production Supabase project once one exists. Live Supabase MCP project listing still shows one Matchmake project (`urrnrcdxekhovsemeuly`, `ACTIVE_HEALTHY`) plus unrelated `FitCheck`; branch listing for the Matchmake project still fails with `Project reference is missing when validating permissions`. Read-only production SQL still shows 7 `Codex%` QA review rows in `public.team_match_reviews`. Next action remains: create/designate a separate Matchmake dev/staging project or branch, point local/dev env files at it, and rerun this confirmation before checking off the item.
  - Still blocked 2026-06-29: local recheck again found root `.env.local`, `.env.local.example`, and `DEPLOYMENT.md` pointing at production project `urrnrcdxekhovsemeuly`; no root `supabase/config.toml`, `.vercel/project.json`, `.mcp.json`, or other local project/branch link was found. The Supabase connector could not complete live project or branch inventory today (`McpServerError: Connection failed.`), and root `.env.local` does not include a `SUPABASE_SERVICE_ROLE_KEY`, so direct read-only production SQL could not be rerun from this shell. Next action remains: create/designate a separate Matchmake dev/staging Supabase project or branch, point local/dev env files at it, re-enable live Supabase verification, and rerun this confirmation before checking off the item.
  - Still blocked 2026-07-01: local recheck found root `.env.local`, `.env.local.example`, and `DEPLOYMENT.md` still pointing at production project `urrnrcdxekhovsemeuly`; worktree env examples that include Supabase URLs also still point at that same production ref. No root `supabase/config.toml`, `.vercel/project.json`, `.mcp.json`, or other local project/branch link was found, and this shell still has no local Supabase CLI, Vercel CLI, or `SUPABASE_ACCESS_TOKEN`. Live Supabase MCP project listing again shows only one Matchmake project (`urrnrcdxekhovsemeuly`, `ACTIVE_HEALTHY`) plus unrelated `FitCheck`; branch listing for the Matchmake project still fails with `Project reference is missing when validating permissions`. Read-only production SQL still shows 7 `Codex%` QA review rows in `public.team_match_reviews`. Next action remains: create/designate a separate Matchmake dev/staging Supabase project or branch, point local/dev env files at it, and rerun this confirmation before checking off the item.
  - Still blocked 2026-07-01 evening recheck: root `.env.local`, `.env.local.example`, and `DEPLOYMENT.md` still point at production project `urrnrcdxekhovsemeuly`; worktree env files/examples that mention Supabase URLs also still point at that same production ref. No root `supabase/config.toml`, `.vercel/project.json`, `.mcp.json`, or other local project/branch link was found, and this shell still has no local Supabase CLI, Vercel CLI, or `SUPABASE_ACCESS_TOKEN`. Live Supabase MCP project listing still shows only one Matchmake project (`urrnrcdxekhovsemeuly`, `ACTIVE_HEALTHY`) plus unrelated inactive `FitCheck`; branch listing still fails with `Project reference is missing when validating permissions`. Read-only production SQL still shows 7 `Codex%` QA review rows in `public.team_match_reviews`. Next action remains: create/designate a separate Matchmake dev/staging Supabase project or branch, point local/dev env files at it, and rerun this confirmation before checking off the item.
  - Still blocked 2026-07-02 recheck: root `.env.local`, `.env.local.example`, and `DEPLOYMENT.md` still point at production project `urrnrcdxekhovsemeuly`; `DEPLOYMENT.md` still documents that local development should prefer a non-production Supabase project once one exists. No root `supabase/config.toml`, `.mcp.json`, `.vercel/project.json`, or other local project/branch link was found. Live Supabase connector listing still shows one active Matchmake project (`urrnrcdxekhovsemeuly`, `ACTIVE_HEALTHY`) plus unrelated inactive `FitCheck`; branch listing still fails with `Project reference is missing when validating permissions`. Read-only production SQL still shows 7 `Codex%` QA review rows in `public.team_match_reviews`. Supabase changelog scan found no new breaking-change item that changes this separation requirement. Next action remains: create/designate a separate Matchmake dev/staging Supabase project or branch, point local/dev env files at it, and rerun this confirmation before checking off the item.
  - Still blocked 2026-07-03 recheck: root `.env.local`, `.env.local.example`, and `DEPLOYMENT.md` still point at production project `urrnrcdxekhovsemeuly`; no root `supabase/config.toml`, `.mcp.json`, `.vercel/project.json`, or other local project/branch link was found. Supabase MCP was reachable (`401` without a token) and the authenticated connector still lists one active Matchmake project (`urrnrcdxekhovsemeuly`, `ACTIVE_HEALTHY`) plus unrelated inactive `FitCheck`; branch listing still fails with `Project reference is missing when validating permissions`. Read-only production SQL still shows 7 `Codex%` QA review rows in `public.team_match_reviews`. Supabase changelog scan found no relevant breaking-change item that changes this separation requirement. Next action remains: create/designate a separate Matchmake dev/staging Supabase project or branch, point local/dev env files at it, and rerun this confirmation before checking off the item.
  - Still blocked 2026-07-04 recheck: root `.env.local`, `.env.local.example`, and `DEPLOYMENT.md` still point at production project `urrnrcdxekhovsemeuly`; no root `supabase/config.toml`, `.mcp.json`, `.vercel/project.json`, or other local project/branch link was found. This shell still has no local Supabase CLI, Vercel CLI, `SUPABASE_ACCESS_TOKEN`, or `VERCEL_TOKEN`. Live Supabase connector listing still shows one active Matchmake project (`urrnrcdxekhovsemeuly`, `ACTIVE_HEALTHY`) plus unrelated inactive `FitCheck`; branch listing still fails with `Project reference is missing when validating permissions`. Read-only production SQL still shows 7 `Codex%` QA review rows in `public.team_match_reviews`. Next action remains: create/designate a separate Matchmake dev/staging Supabase project or branch, point local/dev env files at it, and rerun this confirmation before checking off the item.
  - Still blocked 2026-07-06 recheck: root `.env.local`, `.env.local.example`, and `DEPLOYMENT.md` still point at production project `urrnrcdxekhovsemeuly`; no root `supabase/config.toml`, `.mcp.json`, `.vercel/project.json`, or other local project/branch link was found. This shell still has no local Supabase CLI, Vercel CLI, `SUPABASE_ACCESS_TOKEN`, or `VERCEL_TOKEN`. Supabase MCP is reachable (`401` without a token), and the authenticated connector still lists one active Matchmake project (`urrnrcdxekhovsemeuly`, `ACTIVE_HEALTHY`) plus unrelated inactive `FitCheck`; branch listing still fails with `Project reference is missing when validating permissions`. Read-only production SQL still shows 7 `Codex%` QA review rows in `public.team_match_reviews`. Supabase changelog scan found no relevant breaking-change item that changes this separation requirement. Next action remains: create/designate a separate Matchmake dev/staging project or branch, point local/dev env files at it, and rerun this confirmation before checking off the item.
  - Deferred/skipped 2026-07-07 by launch-owner instruction to stop treating this as a recurring blocker for the narrow MOSEF / HS pilot. This is not a confirmation that production and development data are separated: local `.env.local` still points at production project `urrnrcdxekhovsemeuly`, and broader launch should still move local/dev workflows to a local Supabase stack, free dev project, paid branch, or designated staging project before expansion.
- [ ] Confirm required environment variables are set in production.
  - Blocked/partial 2026-06-25: code/docs inspection identifies the required production Vercel env vars as `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `GEMINI_API_KEY`, and optional `GEMINI_MODEL`; Supabase Edge Function secrets for the weekly Discord calendar are separate (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `DISCORD_SCRIM_WEBHOOK_URL`, optional Discord filters). Vercel MCP confirmed project `matchmake` (`prj_bkHSoerkDBBlaM3kkJQmTlwpqfDm`) has a READY production deployment, but this session has no Vercel env-listing tool or local Vercel CLI. Protected production aliases redirect through Vercel SSO, so unauthenticated runtime probes could not confirm server env behavior; protected fetch could load `/login` but API-route probes still redirected. Next action: verify env names in Vercel Project Settings or via `vercel env ls production`, then add `SUPABASE_SERVICE_ROLE_KEY` to `DEPLOYMENT.md` if production uses the calendar feed route.
  - Still blocked/partial 2026-06-30: current code/docs inspection still identifies the required production Vercel env vars as `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `GEMINI_API_KEY`, and optional `GEMINI_MODEL`; optional extractor controls remain `POSTGAME_EXTRACT_RATE_LIMIT_MAX_REQUESTS`, `POSTGAME_EXTRACT_RATE_LIMIT_WINDOW_MS`, `POSTGAME_EXTRACT_MAX_UPLOAD_BYTES`, and `POSTGAME_EXTRACT_DEBUG_LOGS=false`. Vercel MCP can reach team `pogchews-projects`, project `matchmake` (`prj_bkHSoerkDBBlaM3kkJQmTlwpqfDm`), and latest READY production deployment `dpl_4ioXp2MF6p6vpqr6K5LqpYYm9eCK`, but the available Vercel MCP tools still do not expose environment-variable listing. No local `vercel` CLI or `.vercel/project.json` link exists, and protected production deployment probes redirect through Vercel SSO before app routes run, so production env names/values could not be verified from this session. Next action remains: verify the production env names in Vercel Project Settings or with an authenticated `vercel env ls production`, then check this item off only if all required names are present and scoped to Production.
  - Still blocked/partial 2026-07-01: code/docs inspection still identifies required production Vercel env vars as `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, and `GEMINI_API_KEY`; `GEMINI_MODEL` remains optional but documented, and extractor controls remain optional (`POSTGAME_EXTRACT_RATE_LIMIT_MAX_REQUESTS`, `POSTGAME_EXTRACT_RATE_LIMIT_WINDOW_MS`, `POSTGAME_EXTRACT_MAX_UPLOAD_BYTES`, `POSTGAME_EXTRACT_DEBUG_LOGS=false`). Supabase Edge Function secrets remain separate (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `DISCORD_SCRIM_WEBHOOK_URL`, optional Discord filters and `MATCHMAKE_APP_URL`). Vercel MCP can still reach team `pogchews-projects`, project `matchmake` (`prj_bkHSoerkDBBlaM3kkJQmTlwpqfDm`), and latest READY production deployment `dpl_4ioXp2MF6p6vpqr6K5LqpYYm9eCK`, but the available Vercel MCP tools still do not expose environment-variable listing. This shell has no local `vercel` CLI, no `VERCEL_TOKEN`, and no root `.vercel/project.json`; the local `.env.local` is also missing `SUPABASE_SERVICE_ROLE_KEY`, which is a local gap but does not prove production state. A protected production API probe to `/api/calendar/feed/...` still redirected to Vercel SSO before the app route ran, so runtime env presence could not be inferred. Next action remains: verify the production env names in Vercel Project Settings or with authenticated `vercel env ls production`, then check this item off only if all required names are present and scoped to Production.
  - Still blocked/partial 2026-07-05 recheck: code/docs inspection still identifies required Vercel production env vars as `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, and `GEMINI_API_KEY`, with optional `GEMINI_MODEL` and extractor controls. Vercel connector can confirm project `matchmake` (`prj_bkHSoerkDBBlaM3kkJQmTlwpqfDm`) and production deployment `dpl_4ioXp2MF6p6vpqr6K5LqpYYm9eCK` are reachable, but still does not expose environment-variable rows; this shell still has no `vercel` CLI, `VERCEL_TOKEN`, or `.vercel/project.json`. A protected production runtime probe to `https://matchmake-zeta.vercel.app/api/calendar/feed/aaaaaaaaaaaaaaaaaaaaaaaa` reached the app route and returned `500 {"error":"Calendar feed is not configured."}`, which means the deployed route is missing `NEXT_PUBLIC_SUPABASE_URL` and/or `SUPABASE_SERVICE_ROLE_KEY` at runtime. `/login` loads, but that static page does not prove the required server secrets are configured. Next action remains: set/verify all required Production-scoped Vercel env vars in Project Settings or via authenticated `vercel env ls production`, especially `SUPABASE_SERVICE_ROLE_KEY`, then redeploy or promote a deployment that can return `404 Calendar feed not found` for a bogus calendar token instead of the configuration `500`.
  - Still blocked/partial 2026-07-07 recheck: Supabase connector confirmed production project URL `https://urrnrcdxekhovsemeuly.supabase.co` and an enabled publishable key exist, and Vercel connector confirmed project `matchmake` plus production deployment `dpl_4ioXp2MF6p6vpqr6K5LqpYYm9eCK` are reachable. The available Vercel connector still does not expose env-var rows, this shell still has no `vercel` CLI/`VERCEL_TOKEN`/`.vercel/project.json`, and the Supabase connector does not expose service-role keys. Runtime probe to `https://matchmake-zeta.vercel.app/api/calendar/feed/aaaaaaaaaaaaaaaaaaaaaaaa` still returns `500 {"error":"Calendar feed is not configured."}`; source readback confirms that response happens only when `NEXT_PUBLIC_SUPABASE_URL` or `SUPABASE_SERVICE_ROLE_KEY` is absent from the deployed route. Next action: add or verify `SUPABASE_SERVICE_ROLE_KEY` in Vercel Production settings, keep `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` set to the confirmed production Supabase values, then redeploy/promote and expect a bogus token to return `404 Calendar feed not found`.
- [x] Confirm `GEMINI_API_KEY` quota, billing, and failure behavior.
  - Blocked/partial 2026-06-25: local `.env.local` contains `GEMINI_API_KEY` and `GEMINI_MODEL=gemini-flash-lite-latest`; a low-token live Gemini API smoke test against that model returned HTTP 200 with `OK`, so the local key is valid and not immediately quota-exhausted. App failure behavior is verified in `src/app/api/postgame/extract/route.js`: missing key returns `500`, quota/resource exhaustion returns `429` with `quota_exhausted`, overload and 5xx paths attempt fallback before returning manual-entry-safe errors, and the dashboard UI maps `quota_exhausted`, `model_overloaded`, and `parse_failed` to manual-entry messaging. Official Gemini troubleshooting docs map 429 `RESOURCE_EXHAUSTED` to rate/spend limits, 503 to temporary overload, and 400 `FAILED_PRECONDITION` to cases where billing must be enabled. Still blocked for production launch confirmation: Vercel production env listing is unavailable in this session, protected production API probes redirect through SSO, and the actual billing/quota tier requires Google AI Studio or Google Cloud Console access. Next action: verify production `GEMINI_API_KEY` in Vercel and confirm billing, quota tier, and quota alerts in Google AI Studio or Cloud Console, then rerun this item.
  - Still blocked/partial 2026-06-30: local `.env.local` still contains `GEMINI_API_KEY` and `GEMINI_MODEL=gemini-flash-lite-latest`; a low-token live Gemini API smoke test returned HTTP `200`, text `OK`, and `finishReason: STOP`, so the local key is valid and not immediately quota-blocked. Current app failure behavior remains launch-safe: missing key returns `500`, 429/`RESOURCE_EXHAUSTED` maps to `quota_exhausted`, 503/`UNAVAILABLE` attempts fallback before returning `model_overloaded`, parse failure attempts fallback before returning `parse_failed`, and the dashboard maps those states to manual-entry-safe copy while launch analytics classifies the failure reason. Current Gemini docs still require billing/quota verification in AI Studio or Google Cloud Console: active rate limits are project/tier based, 429 covers request/token/spend limits, 503 is temporary overload, and billing tier/credits/spend caps are visible in AI Studio Billing/Usage. Still blocked for production launch confirmation because this session cannot inspect the production Vercel `GEMINI_API_KEY`, AI Studio billing tier, quota alerts, or spend caps. Next action: verify the production key is set in Vercel, then confirm the linked Gemini project billing tier, available credits or postpay status, quota/rate tier, and quota/spend alerts in AI Studio or Cloud Console.
  - Still blocked/partial 2026-07-01: local `.env.local` still contains `GEMINI_API_KEY` and `GEMINI_MODEL=gemini-flash-lite-latest`; a low-token live Gemini API smoke test against that model returned HTTP `200`, text `OK.`, and `finishReason: STOP`, so the local key is valid and not immediately quota-blocked. Current app failure behavior remains launch-safe: missing key returns `500`, 429/`RESOURCE_EXHAUSTED` or quota text maps to `quota_exhausted`, 503/`UNAVAILABLE` and 5xx/request failures attempt row-only fallback before returning manual-entry-safe overload errors, parse failure attempts fallback before returning `parse_failed`, and the dashboard/launch analytics classify `quota_exhausted`, `model_overloaded`, `parse_failed`, upload, timeout, and unknown failures. Current Gemini docs still require billing/quota verification in AI Studio or Google Cloud Console: active limits are project-based, RPM/TPM/RPD and spend-tier limits can all produce `429 RESOURCE_EXHAUSTED`, billing tier/status and prepay credit balance are visible in AI Studio, and usage is monitored in AI Studio Dashboard > Usage. Still blocked for production launch confirmation because this session cannot inspect the production Vercel `GEMINI_API_KEY`, AI Studio billing tier/status, credit balance or postpay status, active rate limits, quota alerts, or spend alerts. Next action: verify the production key is set in Vercel, then confirm the linked Gemini project billing tier/status, available credits or postpay status, active model quota/rate tier, and quota/spend alerts in AI Studio or Cloud Console.
  - Completed/accepted 2026-07-07 for the narrow MOSEF / HS pilot with a free-tier posture. Local `GEMINI_API_KEY` against `GEMINI_MODEL=gemini-flash-lite-latest` returned HTTP `200` in a low-token smoke test, so the configured key is valid and not immediately quota-blocked. Current Gemini docs confirm the Gemini API has a Free tier with free input/output tokens for limited model access, rate limits are project-based and visible in AI Studio, and moving to paid requires setting up billing; to stay free-tier as long as possible, do not enable paid billing solely for launch, keep screenshot-extraction use narrow, rely on the existing manual-entry fallback for quota/overload/model failures, and revisit billing only if pilot usage repeatedly hits `429 RESOURCE_EXHAUSTED` or school workflows are blocked.
- [x] Confirm backups are enabled for Supabase.
  - Blocked/partial 2026-06-25: Supabase MCP confirms the production Matchmake project `urrnrcdxekhovsemeuly` is `ACTIVE_HEALTHY` on Postgres 17, but the available connector project details do not expose plan, daily-backup status, PITR status, backup retention, or the backup list. Current Supabase docs say daily backups are automatic for Pro, Team, and Enterprise projects; PITR is a separate add-on, replaces daily backups when enabled, and is managed from Dashboard Point in Time settings or the Management API backup endpoints. This session has no Supabase CLI and no local `SUPABASE_ACCESS_TOKEN`/Management API token name available to list backups directly. Existing school deletion/export docs also still list the exact production backup retention period as an open owner decision. Next action: in Supabase Dashboard for project `urrnrcdxekhovsemeuly`, verify Database > Backups has recent restorable backups or Point in Time has an active recovery window, record the retention period, and decide whether daily backups are sufficient for MOSEF launch or PITR should be enabled.
  - Still blocked/partial 2026-06-30: Supabase MCP again confirms the production Matchmake project `urrnrcdxekhovsemeuly` is `ACTIVE_HEALTHY` in `us-east-2` on Postgres `17.6.1.105`, but the available connector metadata still does not expose plan, daily-backup status, PITR status, backup retention, or a backup list. Current Supabase docs still say daily backups are automatic on Pro, Team, and Enterprise projects, while Point-in-Time Recovery is separately managed and replaces daily backups when enabled. This shell has no local `supabase` CLI, no `SUPABASE_ACCESS_TOKEN`, and no `.env.local` Supabase access token for the Management API. Existing school deletion/export docs still list the exact production backup retention period as an open owner decision. Next action remains: open Supabase Dashboard for project `urrnrcdxekhovsemeuly`, verify Database > Backups has recent restorable backups or Point-in-Time Recovery has an active recovery window, record the retention period, and decide whether daily backups are sufficient for MOSEF launch or PITR should be enabled.
  - Still blocked/partial 2026-07-01: Supabase MCP again confirms the production Matchmake project `urrnrcdxekhovsemeuly` is `ACTIVE_HEALTHY` in `us-east-2` on Postgres `17.6.1.105`, but the available connector metadata still does not expose plan, daily-backup status, PITR status, backup retention, or a backup list. Current Supabase docs still say daily backups are automatic for Pro, Team, and Enterprise projects with 7/14/30-day daily-backup access by plan; PITR is a separate add-on, replaces daily backups when enabled, and is managed from Dashboard Point in Time settings. Docs also show backup listing through the Management API endpoint `/v1/projects/$PROJECT_REF/database/backups`, but this shell still has no local `supabase` CLI, no `SUPABASE_ACCESS_TOKEN`, no `.env.local` Supabase access token, and no local `SUPABASE_SERVICE_ROLE_KEY`; the Management API could not be queried directly. Existing school deletion/export docs still list the exact production backup retention period as an open owner decision. Next action remains: open Supabase Dashboard for project `urrnrcdxekhovsemeuly`, verify Database > Backups has recent restorable backups or Point-in-Time Recovery has an active recovery window, record the retention period, and decide whether daily backups are sufficient for MOSEF launch or PITR should be enabled.
  - Deferred/skipped 2026-07-07 by launch-owner instruction to close the recurring launch checklist for the narrow pilot without adding paid services. This is not a confirmation that native Supabase backups or PITR are enabled: the connector confirms production project `urrnrcdxekhovsemeuly` is `ACTIVE_HEALTHY`, but it still does not expose plan, backup list, daily-backup status, PITR status, or retention. Current Supabase docs say native daily backups are automatic only for Pro, Team, and Enterprise projects, with PITR as a paid add-on; free-tier projects should regularly export data with Supabase CLI `db dump` and keep off-site backups. Before broader rollout, verify Database > Backups/PITR in the Supabase Dashboard or implement a manual export cadence with off-site storage.
- [x] Add basic error monitoring or logging strategy.
  - Completed 2026-06-25. Added `docs/production-monitoring-logging-strategy.md` as the launch monitoring runbook: current Vercel Runtime Logs, Supabase logs, Edge Function logs, and `/admin` activity log baseline; critical signals for auth, scrims, extraction, review saves, Discord digest, and admin actions; student-data log hygiene rules; pre-launch, post-deploy, and first-two-weeks review cadence; severity levels; and an upgrade path to Vercel analytics/drains or Sentry once school disclosures and support capacity justify it. No new monitoring vendor or runtime dependency was added for this basic launch strategy.
- [x] Add basic analytics for signup, scrim posting, extraction success, and extraction failures.
  - Completed 2026-06-25. Added a privacy-safe launch analytics RPC backed by `public.admin_activity_logs` plus client-side best-effort tracking for `signup_completed`, `scrim_posted`, `extraction_completed`, and `extraction_failed`. The RPC is exposed only to authenticated users, uses normal RLS insert policy checks instead of a public `SECURITY DEFINER` function, records coarse metadata only (IDs, game title, row counts, fallback/failure category), and avoids emails, screenshot contents, raw model output, and extracted player/stat rows. Applied production migrations `add_launch_analytics_event_rpc` and `secure_launch_analytics_event_rpc` to Supabase project `urrnrcdxekhovsemeuly`; rollback verification inserted exactly 1 analytics event and left 0 residual test rows. Focused QA passed: file-scoped ESLint, `npm run build`, Supabase function/privilege checks, Supabase security advisors, rollback insert/readback, and `git diff --check`.
- [x] Review `DEPLOYMENT.md` and update it with current launch steps.
  - Completed 2026-06-25. Rewrote `DEPLOYMENT.md` for the current MOSEF / high-school launch state: explicit unresolved launch blockers, required and optional Vercel env vars including `SUPABASE_SERVICE_ROLE_KEY` and extractor controls, Supabase SQL/runbook verification order, calendar subscription and weekly Discord setup, source lint/build commands, Vercel deploy steps, post-deploy smoke tests, and monitoring/analytics checks. Focused QA passed with doc readback, required-term checks, and `git diff --check -- DEPLOYMENT.md MOSEF_HS_LAUNCH_TODO.md`; no app lint/build was rerun because this task changed documentation only.

## P1 - Accessibility and Mobile QA

- [x] QA landing/login on mobile and desktop.
  - Completed 2026-06-25. Rendered `/login` on local dev at desktop `1440x900` and mobile `390x844` using the in-app Browser. Verified page identity, non-blank hero content, no framework overlay, no relevant console warnings/errors, no mobile horizontal overflow, and visible first-viewport layout. Exercised login interaction on both viewports: opened the login modal, entered dummy invalid credentials, submitted, stayed on `/login`, and saw the expected `Invalid login credentials` error with the modal still usable. Evidence screenshots: `/tmp/matchmake-login-desktop-landing.png`, `/tmp/matchmake-login-desktop-invalid.png`, `/tmp/matchmake-login-mobile-landing.png`, `/tmp/matchmake-login-mobile-invalid.png`. No code changes were needed.
- [x] QA scrim board and scrim posting on mobile and desktop.
  - Completed 2026-06-26. Exercised the authenticated Scrim Board on local dev at desktop `1280x900` and mobile `390x844` using a temporary QA signup/org/team/listing set, then cleaned all QA auth/user/org/team/scrim/activity rows from Supabase project `urrnrcdxekhovsemeuly` and verified zero remaining rows for marker `codex-qa-1782504717032`.
  - Fixed two issues found during QA: scrim posting could insert the listing but leave the modal open with a client error because launch analytics called `scheduledAt.toISOString()` on an already-serialized scheduled time, and Material Symbols could render as literal icon names on mobile, causing overlapping controls and horizontal overflow. Also made Scrim Board avatars non-shrinking so long school/team names do not compress the card avatar.
  - Browser QA passed after fixes: desktop board rendered with `3 active listings`, `Post LF Scrim` visible, no horizontal overflow, no literal icon-name text, and desktop posting had already advanced the board from 1 to 2 listings without console errors; mobile posting advanced the board from 2 to 3 listings, closed the modal, kept no horizontal overflow, and produced no new console warnings/errors. Evidence screenshots: `/tmp/matchmake-scrim-desktop-posted.png`, `/tmp/matchmake-scrim-mobile-board.png`, `/tmp/matchmake-scrim-mobile-posted.png`.
  - Focused QA passed: `npx eslint src/app/page.js src/components/MaterialSymbol.js`, `npm run build`, Supabase cleanup verification, and browser DOM/viewport checks for desktop and mobile board layout.
- [x] QA org dashboard on mobile and desktop.
  - Completed 2026-06-26. Exercised `/org` on local dev at desktop `1280x900` and mobile `390x844` using a temporary QA signup/org/team marked `codex-org-qa-1782506691726`, then cleaned all QA auth/user/org/team/activity rows from Supabase project `urrnrcdxekhovsemeuly` and verified zero remaining rows for the marker.
  - Fixed one accessibility issue found during QA: the visible `Filter programs` label was not exposed as the native select's accessible name. Added an explicit `aria-label="Filter programs"` to the org dashboard program filter.
  - Hardened org dashboard icons by adding bundled Material Symbol codepoints for `stadia_controller`, `location_city`, and `handshake`, and replaced the missing `location_on` icon with `location_city` so mobile does not fall back to literal icon text.
  - Browser QA passed after fixes: desktop dashboard rendered the QA org/team, stats cards, quick actions, upcoming scrims empty state, and program row; program filtering to `Valorant` worked; the team row expanded to show mode/location/pending/matched/confirmed/rating details; Create Team, Schedule Scrim, and Manage links resolved; mobile top and program/detail views had no horizontal overflow, no literal icon-name text, and no new console warnings/errors during interaction. Expected existing warnings observed before the fix were the Next sticky-header auto-scroll warning and the optional missing `logo_url` column fallback (`Run logo SQL first`).
  - Evidence screenshots: `/tmp/matchmake-org-dashboard-desktop.png`, `/tmp/matchmake-org-dashboard-mobile-top.png`, `/tmp/matchmake-org-dashboard-mobile-programs.png`.
  - Focused QA passed: `npx eslint src/app/org/page.js src/components/MaterialSymbol.js`, `npm run build`, Supabase cleanup verification, and browser DOM/viewport checks for desktop and mobile org dashboard layout.
- [x] QA team dashboard, Team Stats, Deep Stats, and roster management on mobile and desktop.
  - Completed 2026-06-26. Exercised `/team?id=<team_id>` on local dev at desktop `1280x900` and mobile `390x844` using a temporary QA signup/org/team marked `codex-team-qa-1782507508820`, plus two temporary saved Valorant match reviews seeded for Team Stats/Deep Stats coverage. Cleaned all QA auth/user/org/team/review/activity rows from Supabase project `urrnrcdxekhovsemeuly` and verified zero remaining rows for the marker; the cleanup had to temporarily disable and re-enable only `public.users` trigger `users_admin_activity_after_write` because that existing trigger can write a delete log referencing the just-deleted user.
  - Fixed one roster-management issue found during QA: after saving roster profiles, the `Roster saved.` confirmation was immediately cleared because the selected-team effect reran when the team object updated. Added a ref-guarded selected-team context check so roster state resets only when switching teams/game context.
  - Browser QA passed after the fix: desktop rendered team header, Team Stats with `2 Reviews`, Deep Stats (`KDA Shape`, `Combat Score`, `Econ Rating`), game history, and expanded roster management; roster profile edits persisted and the `Roster saved.` message remained visible. Mobile rendered Team Stats/Deep Stats, game history, and expanded roster management without horizontal overflow, literal icon-name text, framework overlay, or new console warnings/errors.
  - Evidence screenshots: `/tmp/matchmake-team-dashboard-desktop.png`, `/tmp/matchmake-team-dashboard-mobile-stats.png`, `/tmp/matchmake-team-dashboard-mobile-roster.png`.
  - Focused QA passed: `npx eslint src/app/team/page.js`, `npm run build`, Supabase cleanup verification, and browser DOM/viewport checks for desktop and mobile team dashboard layout.
- [x] QA requests page on mobile and desktop.
  - Completed 2026-06-30. Exercised `/requests` on local dev at desktop `1280x900` and mobile `390x844` using a temporary two-org Valorant request fixture marked `codex-requests-qa-1782858198452`, then cleaned all QA auth/user/org/team/scrim/activity rows from Supabase project `urrnrcdxekhovsemeuly` and verified zero remaining rows.
  - Fixed one RLS data-access issue found during QA: authenticated request participants could read the relevant `scrim_requests` rows but nested counterpart team/org data rendered as `Unknown Team` / `Independent`. Updated production and repo `rls_policies.sql` so authenticated users can read team/org metadata for teams participating in scrims they already participate in, using the existing private helper functions rather than reopening broad public access. Updated the production RLS runbook summary to match.
  - Fixed one mobile UI issue found during QA: `move_to_inbox` and `outbox` rendered as literal text in the request tab bar because the local Material Symbols map was missing those bundled font codepoints. Added `move_to_inbox` (`e168`) and `outbox` (`ef5f`) to `src/components/MaterialSymbol.js`.
  - Browser QA passed after fixes: desktop rendered Needs Reply `1`, Waiting `1`, Confirmed `1` with real counterpart org/team names and no horizontal overflow; accepting the inbound request showed `Request accepted.`, dropped Needs Reply to `0`, and raised Confirmed to `2`; mobile rendered the remaining Waiting and Confirmed cards with real names, no literal icon-name text, no horizontal overflow, and usable bottom navigation.
  - Evidence screenshots: `/tmp/matchmake-requests-desktop-before.png`, `/tmp/matchmake-requests-desktop-after-accept.png`, `/tmp/matchmake-requests-mobile-after-icon-fix.png`.
  - Focused QA passed: `npx eslint src/app/requests/page.js src/components/MaterialSymbol.js`, `npm run build`, live `pg_policies` readback for the updated org/team policies, Supabase security advisors, Supabase cleanup verification, and `git diff --check -- MOSEF_HS_LAUNCH_TODO.md rls_policies.sql src/components/MaterialSymbol.js docs/supabase-production-rls-policy-runbook-2026-06-17.md`.
- [x] QA calendar page on mobile and desktop.
  - Completed 2026-06-30. Exercised `/calendar` on local dev at desktop `1280x900` and mobile `390x844` using a temporary two-org Valorant calendar fixture marked `codex-calendar-qa-1782859093617`, then cleaned all QA auth/user/org/team/scrim/activity rows from Supabase project `urrnrcdxekhovsemeuly` and verified zero remaining rows.
  - Fixed two calendar icon issues found during QA: open scrim status chips rendered literal `campaign`, and the expanded Discord notification accordion rendered literal `expand_less`. Added bundled Material Symbols codepoints `campaign` (`ef49`) and `expand_less` (`e5ce`) to `src/components/MaterialSymbol.js`.
  - Browser QA passed after fixes: desktop rendered three selected-day scrims across Open/Pending/Confirmed, the Confirmed status filter reduced the day to one confirmed scrim, the calendar integration panel showed the expected SQL-setup fallback because production still lacks `organizations.calendar_feed_token`, and the Discord preview showed the confirmed scrim without posting to Discord. Mobile rendered the same three scrim cards plus the integration fallback with no horizontal overflow, no literal icon-name text, and usable bottom navigation.
  - Evidence screenshots: `/tmp/matchmake-calendar-desktop-confirmed-export.png`, `/tmp/matchmake-calendar-mobile-export.png`.
  - Focused QA passed: `npx eslint src/app/calendar/page.js src/components/MaterialSymbol.js`, `npm run build`, Supabase cleanup verification, and browser DOM/viewport checks for desktop and mobile calendar layout.
- [x] Check keyboard navigation for major forms and dialogs.
  - Completed 2026-07-01. Added focus management to the login dialog and post-scrim dialog: opening now moves focus into the dialog, Tab/Shift+Tab stay within the dialog, Escape closes, and focus returns to the opener. Added dialog semantics to the post-scrim sheet with `role="dialog"`, `aria-modal="true"`, and `aria-labelledby="post-scrim-title"`.
  - Browser QA passed on local Chromium: login modal opened with focus in Email, Tab order stayed inside Email -> Password -> Log in -> Create an account -> Close -> Email, Shift+Tab wrapped back to Close, and Escape returned focus to the Log In opener; signup form tabbed through Display name -> Organization name -> Email -> Password -> Create account -> Log in. The unauthenticated `/?post=true` route rendered the public landing/login state rather than the post dialog, so post-scrim dialog behavior was source-validated and lint/build-validated without creating production auth data solely for this keyboard pass.
  - Evidence screenshots: `/tmp/matchmake-keyboard-login-dialog.png`, `/tmp/matchmake-keyboard-signup-form.png`.
  - Focused QA passed: `npx eslint src/app/login/page.js src/app/page.js`, `npm run build`, bundled-Playwright Chromium keyboard checks for `/login` and `/signup`, unauthenticated `/?post=true` route check, `git diff --check -- src/app/login/page.js src/app/page.js`, direct TODO readback, and direct trailing-whitespace checks for touched files.
- [x] Check color contrast for primary dashboard cards, status badges, and error states.
  - Completed 2026-07-01. Audited the dashboard/status/error color pairs in the Matchmake theme tokens and primary dashboard surfaces: primary cards (`primary-fixed`/`on-primary-fixed` and `primary` text variants), request/calendar/team status chips, success chips, and error containers all meet at least 4.5:1 in light and dark token calculations.
  - Fixed one dashboard contrast miss found during the audit: the Team Stats Damage Conversion card used hardcoded `#D97706` for small Objective Damage text, which was only about 3.19:1 on light cards. Replaced it with adaptive `text-tertiary`/`bg-tertiary`, which raises the light-mode text contrast to about 6.73:1 and keeps dark-mode contrast above 4.5:1.
  - Browser QA passed for reachable public rendered states: `/login` and `/signup` loaded with meaningful content, no framework overlay, no console warnings/errors, and screenshot evidence. Authenticated dashboard routes redirected to `/login` in this unauthenticated browser session, so dashboard-specific contrast was source/token-audited and lint/build-validated without creating production auth data solely for this visual pass.
  - Evidence screenshots: `/tmp/matchmake-contrast-login-light.png`, `/tmp/matchmake-contrast-signup-light.png`.
  - Focused QA passed: contrast ratio calculation script for audited pairs, Browser page identity/blank/console/screenshot checks, `npx eslint src/app/team/page.js src/app/requests/page.js src/app/calendar/page.js src/app/org/page.js src/app/page.js`, `npm run build`, `git diff --check -- src/app/team/page.js`, direct TODO readback, and direct trailing-whitespace checks for touched files.

## P1 - MOSEF Onboarding and Support

- [x] Create a short coach/admin onboarding guide.
  - Completed 2026-07-01. Added `docs/coach-admin-onboarding-guide.md`, a 100-line pilot guide for coaches, esports leads, and school administrators covering pre-launch prep, account/org/team setup, first-practice scrim workflow, roster/profile-link expectations, safety/reporting expectations, weekly coach checks, launch limitations, and first-week success criteria.
  - Focused QA passed: readback of the guide, verified referenced docs exist (`docs/profile-link-safety-guidance.md`, `docs/inappropriate-behavior-reporting-escalation.md`, `docs/admin-removal-guidance.md`), scanned for unresolved placeholders/TODO markers, and checked trailing whitespace for the new guide.
- [x] Create a first-team setup guide.
  - Completed 2026-07-01. Added `docs/first-team-setup-guide.md`, a 101-line MOSEF pilot guide covering first-team prerequisites, `/team/new` setup fields, game/mode/rank/location choices, organization prerequisite handling, post-create `/org` and `/team` verification, Roster Management completion, profile-link safety, common setup mistakes, and readiness criteria for first-scrim work.
  - Focused QA passed: readback of the guide, verified referenced docs exist (`docs/coach-admin-onboarding-guide.md`, `docs/profile-link-safety-guidance.md`), scanned for unresolved TODO/TBD/placeholders and dangling next-guide path, and checked trailing whitespace for the new guide.
- [x] Create a first-scrim posting guide.
  - Completed 2026-07-01. Added `docs/first-scrim-posting-guide.md`, a 117-line MOSEF pilot guide covering first-scrim prerequisites, Post Scrim fields, team/game locking, date/time/time-zone handling, game count, location, opponent rank range, public notes safety, listing verification, request states, chat coordination, calendar checks, after-scrim handoff, common first-post mistakes, and readiness criteria for first-match work.
  - Focused QA passed: readback of the guide, verified referenced docs exist (`docs/first-team-setup-guide.md`, `docs/inappropriate-behavior-reporting-escalation.md`), checked current source for own-listing edit/retract behavior before documenting it, scanned for unresolved TODO/TBD/placeholders, and checked trailing whitespace for the new guide.
- [x] Create a match review upload guide.
  - Completed 2026-07-01. Added `docs/match-review-upload-guide.md`, a 113-line MOSEF pilot guide covering match-review prerequisites, Team Stats `Add Match Review` navigation, screenshot upload, extraction review, editable-field checks, team swap handling, save/update behavior, saved-review verification, source screenshot retention, extraction failure fallback, and common upload mistakes.
  - Focused QA passed: readback of the guide, verified referenced docs exist (`docs/first-scrim-posting-guide.md`, `docs/inappropriate-behavior-reporting-escalation.md`), checked current source for `Add Match Review`, upload, extraction status/error, edit, swap, and save labels before documenting them, scanned for unresolved TODO/TBD/placeholders, and checked trailing whitespace for the new guide and TODO.
- [x] Create a support/contact process for MOSEF users.
  - Completed 2026-07-01. Added `docs/mosef-support-contact-process.md`, a 166-line MOSEF pilot operating guide covering routine support intake, contact paths, support vs safety/privacy/admin-removal routing, required request details, priority levels and response targets, support-record workflow, status labels, privacy handling, coach-facing acknowledgement language, and pre-launch readiness. Also linked it from `docs/coach-admin-onboarding-guide.md` and `docs/match-review-upload-guide.md`.
  - Focused QA passed: readback of the support process and linked sections, verified referenced docs exist, scanned touched docs for unresolved TODO/TBD/placeholders, checked support-process discoverability links, and checked trailing whitespace for the touched docs and TODO. No app lint/build/typecheck was run because this was docs-only.
- [x] Create a fall pilot feedback form or feedback collection plan.
  - Completed 2026-07-01. Added `docs/fall-pilot-feedback-collection-plan.md`, a 215-line MOSEF pilot plan and form specification covering collection goals, channels, submitter roles, timing cadence, form sections/questions, data to avoid, feedback triage categories, severity levels, monthly readout, pre-launch setup checklist, and readiness criteria. Also linked it from `docs/coach-admin-onboarding-guide.md` and `docs/mosef-support-contact-process.md`.
  - Focused QA passed: readback of the feedback plan and linked sections, verified referenced docs exist (`docs/mosef-support-contact-process.md`, `docs/inappropriate-behavior-reporting-escalation.md`, `docs/coach-admin-onboarding-guide.md`), scanned touched docs for unresolved TODO/TBD/placeholders, checked feedback-plan discoverability links, and checked trailing whitespace for the touched docs and TODO. No app lint/build/typecheck was run because this was docs-only.

## P2 - Public Marketing and Trust Polish

- [x] Update landing copy to clearly say it is built for high school esports teams.
  - Completed 2026-07-01. Updated public landing copy in `src/app/login/page.js` so unauthenticated visitors see high-school-specific messaging in the first viewport: `Find better high school scrims.`, `Matchmake helps high school esports teams...`, and `Built for high school esports programs.` Also updated the currently unused `src/components/LandingPage.js` fallback/alternate landing copy to the same high-school positioning.
  - Focused QA passed: source readback and copy search, `npx eslint src/app/login/page.js src/components/LandingPage.js`, `npm run build` (passes with the existing multiple-lockfile workspace-root warning), in-app browser rendered QA on `http://127.0.0.1:3001/login` for desktop and mobile widths, verified no fresh console warnings/errors or framework overlay, verified hero high-school copy is visible, and verified the hero `Get Started` CTA navigates to `/signup`.
- [x] Add supported games to the public-facing page.
  - Completed 2026-07-01. Added a `Supported games` band to the actual unauthenticated public page in `src/app/login/page.js`, sourced from `GAME_OPTIONS` and displaying Valorant, Call of Duty, Counter-Strike 2, League of Legends, Rocket League, Overwatch 2, Marvel Rivals, Deadlock, Super Smash Bros. Ultimate, and Honor of Kings. Also expanded the alternate/fallback `src/components/LandingPage.js` game strip to the same complete game list.
  - Focused QA passed: source readback and supported-game copy search, `npx eslint src/app/login/page.js src/components/LandingPage.js`, `npm run build` (passes with the existing multiple-lockfile workspace-root warning), in-app browser rendered QA on `http://127.0.0.1:3002/login` for desktop and mobile widths, verified all expected game names render, verified no fresh console warnings/errors or framework overlay, and verified the hero `Get Started` CTA still navigates to `/signup`.
- [x] Explain screenshot extraction in simple, trustworthy language.
  - Completed 2026-07-01. Added a public `Screenshot extraction` section to `src/app/login/page.js` explaining the flow as upload scoreboard -> editable stats draft -> coach review before saving, with explicit language that extraction can miss or misread details and is not an official scorebook. Also updated `src/components/LandingPage.js` alternate/fallback extraction copy to say screenshots draft visible stats and coaches review/edit before saving.
  - Focused QA passed: source readback and extraction-copy search, `npx eslint src/app/login/page.js src/components/LandingPage.js`, `npm run build` (passes with the existing multiple-lockfile workspace-root warning), in-app browser rendered QA on `http://127.0.0.1:3003/login` for desktop and mobile widths, verified the new extraction copy renders without horizontal mobile overflow, verified no fresh console warnings/errors or framework overlay, and verified the scoped header `Get Started` link still navigates to `/signup`.
- [x] Add school-safe/privacy positioning to the public-facing page.
  - Completed 2026-07-01. Added a `School-safe privacy` public trust band to `src/app/login/page.js` with coach-supervised positioning, limited public scrim detail language, and no student-data ads/sale/commercial-profile language grounded in the launch privacy docs. Mirrored the same school-safe privacy positioning in the alternate/fallback `src/components/LandingPage.js` component.
  - Focused QA passed: source readback and school-safe/privacy copy search, `npx eslint src/app/login/page.js src/components/LandingPage.js`, `npm run build` (passes with the existing multiple-lockfile workspace-root warning), in-app browser rendered QA on `http://127.0.0.1:3004/login` for desktop and mobile widths, verified the new section/cards render without horizontal mobile overflow, verified no fresh console warnings/errors or framework overlay, and verified the scoped header `Get Started` link still navigates to `/signup`.
- [x] Add a clear "Start pilot" or "Request school access" CTA.
  - Completed 2026-07-02. Updated the actual public route in `src/app/login/page.js` so the header and hero CTAs say `Request school access`, and added a `Fall pilot access` CTA band that points coaches and school esports leads to the existing `/signup` flow. Also aligned the alternate/fallback `src/components/LandingPage.js` CTAs with the same request-access language.
  - Focused QA passed: source readback and CTA-copy search, `npx eslint src/app/login/page.js src/components/LandingPage.js`, `npm run build` (passes with the existing multiple-lockfile workspace-root warning), in-app browser rendered QA on `http://127.0.0.1:3005/login` for desktop and mobile widths, verified three public `Request school access` links render on desktop, verified the mobile hero and pilot CTA buttons fit without horizontal overflow, verified no fresh console warnings/errors or framework overlay, and verified the scoped header `Request school access` link navigates to `/signup`.

## P2 - Admin and Reporting Concepts

- [x] Define a MOSEF/admin reporting dashboard concept.
  - Completed 2026-07-02. Added `docs/mosef-admin-reporting-dashboard-concept.md`, a 268-line concept for a MOSEF admin reporting dashboard grounded in the existing owner-only `/admin` surface. It defines purpose, users, current baseline, dashboard areas, data sources, privacy/access rules, information architecture, operating cadence, implementation phases, deferred decisions, and ready criteria.
  - Focused QA passed: readback of the concept doc, verified it references current `/admin` controls such as `is_matchmake_owner`, bounded record sets, and `admin_activity_logs`, verified it keeps metric definitions, abuse/report queue behavior, and launch-success thresholds deferred to later checklist items, checked for non-ASCII punctuation, and checked trailing whitespace. No app lint/build/typecheck was run because this was docs-only.
- [x] Track active schools/orgs, teams, posted scrims, completed scrims, and extraction usage.
  - Completed 2026-07-02. Updated `src/components/admin/AdminDashboard.js` overview with a `MOSEF pilot tracking` strip using the existing owner dashboard data load: active schools/orgs, teams with scrim/review activity, posted scrims, completed scrims, extraction usage, and extraction review/failure attention. The implementation uses current bounded `/admin` queries and `admin_activity_logs` analytics events; no schema changes or extractor/review-save flow changes were made.
  - Focused QA passed: source readback and tracking-copy search, `npx eslint src/components/admin/AdminDashboard.js`, `npm run build` (passes with the existing multiple-lockfile workspace-root warning), and in-app browser smoke QA on `http://127.0.0.1:3000/admin` plus `/admin/login`. Browser QA confirmed the protected `/admin` route still redirects without an owner session, `/admin/login` renders without console errors or framework overlay, and the harmless `Return to Matchmake` link responds; private dashboard visual QA was not possible without an owner-authenticated browser session.
- [x] Define an abuse/report queue concept.
  - Completed 2026-07-02. Added `docs/abuse-report-queue-concept.md`, a 332-line concept for a restricted abuse/report queue that connects the existing school-first reporting process, support inbox, `/admin` moderation controls, `admin_activity_logs`, and the required restricted case log. It defines purpose, users, queue scope, intake sources, minimum case fields, statuses, severity model, workflow, admin-dashboard relationship, future data-model concept, access/privacy rules, MOSEF-safe metrics, implementation phases, deferred decisions, and ready criteria.
  - Focused QA passed: readback of the concept doc, verified required headings and references to current `/admin` controls and related reporting/removal/support docs, updated the dashboard concept's deferred queue reference to point at the new doc, checked for non-ASCII characters and trailing whitespace, and ran `git diff --check` for the touched docs/TODO paths. No app lint/build/typecheck was run because this was docs-only.
- [x] Define launch metrics for fall pilot success.
  - Completed 2026-07-02. Added `docs/fall-pilot-launch-success-metrics.md`, a 252-line MOSEF fall pilot scorecard defining decision windows, Green/Yellow/Red states, a north-star full-workflow definition, concrete thresholds for adoption, scrim activity, match review/extraction usage, support/feedback, trust/safety/privacy, and reliability, plus expansion gates, a weekly readout template, data-source map, privacy rules, known gaps, and ready criteria.
  - Focused QA passed: readback of the metrics doc, required heading/reference search, verified links to current `/admin`, `admin_activity_logs`, `scrim_requests`, `team_match_reviews`, support/feedback/reporting sources, updated the dashboard concept's launch-metrics references to point at the new doc, checked for non-ASCII characters and trailing whitespace, and ran `git diff --check` for the touched docs/TODO paths. No app lint/build/typecheck was run because this was docs-only.

## Final Launch Gate

- [x] Run full build.
  - Completed 2026-07-02. Ran the repo full production build with `npm run build` (`next build`). The build compiled successfully, ran Next's lint/type validity phase, generated all 19 static pages, finalized page optimization, and collected build traces.
  - QA note: build passed with the existing multiple-lockfile workspace-root warning (`/Users/nicholaschiu/package-lock.json` plus this repo's `package-lock.json`). No code changes were made for this item.
- [x] Run focused lint checks.
  - Completed 2026-07-02. Ran `npx eslint src` as the focused source lint check for the app code; it passed cleanly across 45 source files.
  - QA note: used the source-scoped lint command instead of plain repo-wide `npm run lint` because the repo has generated `.claude/worktrees` content and this checklist's accepted pattern is source-scoped lint plus the already completed full build. No code changes were made for this item.
- [x] Run dependency audit.
  - Completed 2026-07-02. Ran `npm audit --omit=dev --audit-level=moderate` and `npm audit --audit-level=moderate`; both reported the same unresolved production-impacting finding: 2 moderate vulnerabilities from `next@15.5.19` depending on nested `postcss@8.4.31` (`GHSA-qx2v-qp2m-jg93`, vulnerable range `<8.5.10`). The root `postcss` package is already `8.5.10`.
  - Audit remediation note: did not run `npm audit fix --force` because npm proposes installing `next@9.3.3`, a breaking major downgrade from the current Next 15 line. This remains an unresolved launch-readiness risk requiring either an upstream Next patch/override strategy or explicit owner acceptance before final readiness.
- [x] Run authenticated browser smoke test for core flows.
  - Blocked 2026-07-02. In-app browser QA on `http://127.0.0.1:3000` could not run the authenticated core-flow smoke because the browser session is not signed in: protected routes `/`, `/team`, `/requests`, `/calendar`, and `/org` all redirected to `/login`, and a guarded cookie-name check found no `matchmake-access-token` or other auth cookies.
  - Partial QA completed: verified the unauthenticated redirect behavior for the protected core routes, verified the public login page rendered without console warnings/errors or framework overlay, and opened the login dialog successfully. To complete this gate, sign in to Matchmake in the in-app browser with a valid pilot/test account, then rerun the protected-route smoke for `/team`, `/requests`, `/calendar`, and `/org`.
  - Still blocked 2026-07-02 recheck. Re-ran in-app browser QA on `http://127.0.0.1:3000`; protected routes `/`, `/team`, `/requests`, `/calendar`, and `/org` still redirect to `/login`, the guarded cookie-name check still returns no auth cookies, and no console warnings/errors or framework overlay appeared during the redirect checks.
  - Still blocked 2026-07-02 second recheck. Re-ran in-app browser QA against the protected core routes; `/`, `/team`, `/requests`, `/calendar`, and `/org` all still redirected to `/login`, the login page rendered with meaningful content and no framework overlay, browser console warnings/errors were empty, and browser storage keys were empty. Completion still requires signing in to Matchmake in the in-app browser with a valid pilot/test account, then rerunning the authenticated route smoke.
  - Completed 2026-07-02 after signing in with a valid account in the in-app Browser. Authenticated smoke initially found a real `/team` runtime overlay: `ReferenceError: useRef is not defined` in `src/app/team/page.js`. Fixed the missing React hook import, then reran the authenticated smoke for `/`, `/team`, `/requests`, `/calendar`, and `/org`; all routes loaded authenticated content, none redirected to `/login`, no framework overlays remained, and Browser console warnings/errors were empty.
  - Focused QA passed: in-app Browser sign-in and protected-route smoke, settled Team page screenshot after the fix, `npx eslint src/app/team/page.js`, and `npm run build` (passes with the existing multiple-lockfile workspace-root warning). No screenshot extraction review/save flow changes were made.
- [x] Run extractor UI QA for all five priority games.
  - Completed 2026-07-02. Ran actual browser UI upload/extraction QA for the five priority games using the signed-in Missouri Esports account and account-visible teams: Valorant (`Vally High Valorant`), League of Legends (`Valley High School`), Overwatch 2 (`Valley OW`), Marvel Rivals (`Valley MR`), and Deadlock (`Valley Deadlock`). Each dashboard upload reached the extractor, completed without an `Extraction issue:`, opened the review editor, and populated recognizable game-specific output: Valorant (`PEARL`, `LTS michey`, `Neon`, `245`, `17`), League (`Riven`, `Diana`, `Akali`, `Victory`, `15`), Overwatch 2 (`Circuit Royal`, `Junker Queen`, `Junkrat`, `1,928`, `2,241`), Marvel Rivals (`Victory`, `Damage`), and Deadlock (`Duration 51:50`, `Ceejay`, `McGinnis`, `55,777`, `62,114`).
  - Focused QA passed: temporary actual-browser CDP harness in `/tmp/matchmake-extractor-ui-smoke.mjs`, downloaded fixture files in `/tmp`, `npm run verify:postgame-regression`, and direct readback/whitespace checks. No match reviews were saved, no production test teams were created, and no extractor API or screenshot review/save code was changed. Notes: the original local fixture image files were missing; League used a public Riot/ComicBook scoreboard image converted from WebP to JPEG to satisfy strict image validation, and Marvel used a public ONE Esports postgame scoreboard image because the earlier Reddit fixture CDN returned `403`.
- [x] Confirm production RLS policies are applied.
  - Completed 2026-07-02. Verified the active production Supabase project `urrnrcdxekhovsemeuly` directly through the Supabase connector. `pg_class` readback showed RLS enabled on all checked `public`/`private` app tables, with zero disabled tables in those schemas; `pg_policies` readback found no literal always-true `qual` or `with_check` policies. Policy predicates for users, organizations, teams, scrim requests, scrim messages, match reviews, admin activity logs, and `private.owner_accounts` are scoped to own profile/org/team participation, open scrim listings, or Matchmake-owner checks.
  - Focused QA passed: production `pg_policies`/grant/RLS-enabled readbacks, Supabase security advisor readback, anon role simulation, and authenticated non-owner role simulation inside rolled-back transactions. Anon visibility matched the public board subset (13 open unmatched scrims, 10 teams, 5 orgs; zero reviews/messages), and the authenticated non-owner simulation saw one own profile/org/team/review plus zero admin logs. Remaining security advisor notices are the known `private.owner_accounts` info lint for RLS-with-no-policy and leaked-password protection disabled; neither indicates the broad MVP public policies are still present.
- [x] Confirm privacy/terms/school docs are ready.
  - Blocked/partial 2026-07-02. The school launch document set exists and covers the needed review areas: privacy policy, Terms, AUP, DPA template, deletion/export, under-13/COPPA handling, no-sale/no-ads language, behavior reporting/escalation, admin removal, support/contact process, onboarding, first-team setup, first-scrim posting, match-review upload, and feedback collection. Focused doc QA found no missing local `docs/*.md` cross-references among the checked docs.
  - Cannot mark publication/signature readiness complete yet. The current files still contain bracketed placeholders and explicit pre-launch review gates for legal entity name, privacy/support/reporting contacts, mailing address, pilot fee/order-form terms, limitation of liability, indemnity, governing law/dispute terms, DPA security/breach/retention/audit schedules, subprocessor list, privacy request tracker owner, restricted case-log location, incident owners, business hours, school escalation contacts, feedback form owner/link, and counsel/school approval. This gate should stay unchecked until those owner/counsel/school decisions are filled in or formally accepted as launch exceptions.
  - Added `docs/privacy-terms-school-docs-readiness-checklist.md` to consolidate the remaining publication/signature decisions and ready criteria for this blocked gate.
  - Still blocked 2026-07-02 recheck. Re-read the readiness checklist and scanned the source docs for unresolved placeholders and explicit publication gates; the same owner/counsel/school decisions remain open, so this item cannot be checked off without new launch-owner decisions or a documented exception.
  - Deferred/skipped 2026-07-02 by launch-owner instruction to continue the final gate. This is not a confirmation that the docs are publication/signature ready; it records an accepted launch exception/deferment so the checklist can proceed to the next final-gate item.
- [x] Confirm support owner and escalation path.
  - Blocked/partial 2026-07-02. The operating model exists in `docs/mosef-support-contact-process.md` and `docs/inappropriate-behavior-reporting-escalation.md`: routine support path, school-first safety routing, severity levels, support/case record workflow, privacy handling, admin-removal routing, and current product limitations are documented.
  - Cannot confirm launch readiness yet because the docs still require final named details: support inbox, support process owner, backup owner, incident response owner, backup incident owner, reporting contact, business hours, pilot escalation contact, approved restricted support log, approved restricted case-log location, and primary/backup school safety or MOSEF contacts for every pilot school. This item should stay unchecked until those owners/channels are named or explicitly accepted as launch exceptions.
  - Added `docs/support-owner-escalation-readiness-checklist.md` to consolidate the remaining named-owner, contact, log, business-hours, school-contact, and escalation decisions for this blocked gate.
  - Still blocked 2026-07-02 recheck. Re-read the support owner readiness checklist and source docs; no final support inbox, reporting contact, named owners/backups, business hours, restricted support/case logs, or pilot-school contacts are present yet, so this item cannot be checked off without new launch-owner details or a documented exception.
  - Still blocked 2026-07-02 evening recheck. Re-read `docs/mosef-support-contact-process.md`, `docs/inappropriate-behavior-reporting-escalation.md`, and `docs/support-owner-escalation-readiness-checklist.md`; the support/escalation operating model is documented, but the final support inbox/reporting contact, named support and incident owners/backups, staffed hours, restricted log locations/access lists, and pilot-school contacts are still absent or placeholders.
  - Deferred/skipped 2026-07-02 by launch-owner instruction to continue the final gate. This is not a confirmation that support owner/escalation is launch-ready; it records an accepted launch exception/deferment so the checklist can proceed to the final readiness note.
- [x] Prepare final launch readiness note for MOSEF / HS pilot.
  - Completed 2026-07-02. Added `docs/mosef-hs-pilot-final-launch-readiness-note.md`, an exception-based final readiness note that summarizes verified final-gate QA, accepted launch exceptions for privacy/docs and support ownership, the unresolved Next/PostCSS audit risk, and the remaining unchecked production-readiness items.
  - Focused QA passed: readback of the readiness note, required-section and unresolved-placeholder searches, local docs reference checks, trailing-whitespace checks, and `git diff --check` for the touched docs/TODO paths. No app lint/build/typecheck was run because this was docs-only.
